[Unit]
Description=Blink Service
After=basic.target

[Service]
User=root
Type=simple
ExecStart=/usr/bin/blink
ExecStop=/bin/kill $MAINPID
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
