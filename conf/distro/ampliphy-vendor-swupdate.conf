require conf/distro/ampliphy-vendor.conf

DISTRO = "ampliphy-vendor-swupdate"
DISTRO_NAME = "ampliPHY Vendor (Phytec Vendor Distribution + SWUpdate)"

# Add distro feature rauc. The OVERRIDE must be named differently, as there is
# already a package with the same name.
DISTRO_FEATURES:append = " swupdate"
DISTROOVERRIDES:append = ":swupdate-ab"
